node_modules/
.DS_Store
*.log
.env
.claude/
.mcp.json

# Generated by install.sh / install.ps1
host/native-host-wrapper.sh
host/native-host-wrapper.cmd
host/io.openbrowser.mcp.json

# Extension signing key — only the PUBLIC key belongs in the repo (manifest.json "key").
key.pem
*.der

# Downloaded page source (sources_download default outDir)
source/
.openbrowser/
